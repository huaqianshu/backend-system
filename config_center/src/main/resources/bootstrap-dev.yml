spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/huaqianshu/backend-system
          username:
          password:
          search-paths: respo
        default-label: master
    bus:
      trace:
        enabled: true
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: root
    password: root123456
