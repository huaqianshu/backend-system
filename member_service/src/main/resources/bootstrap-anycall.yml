spring:
  application:
    name: memberservice
    cloud:
      config:
        password:
        username:
        uri:  http://localhost:8002/
    profiles:
      active: respo
server:
  port: 9000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/